Python은 ,이 중요함
스크립트 언어는 컴파일언어와 달리 데이터 타입을 정하지 않고 사용하나 없다
는건 아님(무수함) > 숫자?(정수?, 실수?) or 문자?

dict()
: 파이선에서 빈 Dictionary 만듦 {'Key1':'Value', ...} // 다른언어에서 hash

list
: 배열 //[]

for문은 가급적이면 3개이하 (3차원 이상부턴 판단하기 어려움)

실시간(realtime) streaming data(빅데이터가 처리하는거)

!echo 는 !는 쉘명렁어실행

regex
: parsing 할때 쓰이는 라이브러리

